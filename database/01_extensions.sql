-- ═══════════════════════════════════════════════════════
-- MedMap AI — 01: Enable Required Extensions
-- Run this FIRST in Supabase SQL Editor
-- ═══════════════════════════════════════════════════════

CREATE EXTENSION IF NOT EXISTS vector;
CREATE EXTENSION IF NOT EXISTS pg_trgm;
